// UserRole enum removed — role is now a plain String on the User model.
// Lowercase string values: "customer" | "cashier" | "kitchen" | "manager" | "owner" | "super_admin"

enum OrderStatus {
  PLACED
  ACCEPTED
  PREPARING
  READY
  OUT_FOR_DELIVERY
  DELIVERED
  CANCELLED
}

enum PaymentStatus {
  PENDING
  COMPLETED
  FAILED
  REFUNDED
}

enum ReservationStatus {
  PENDING
  APPROVED
  REJECTED
  CANCELLED
}
