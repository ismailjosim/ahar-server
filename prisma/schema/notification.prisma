model Notification {
  id         String   @id @default(uuid())
  type       String
  severity   String
  title      String
  message    String
  read       Boolean  @default(false)
  sourceType String?
  sourceId   String?
  createdAt  DateTime @default(now())
}
